PORT=3000
DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=
# Prisma connection string, e.g.:
# postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=verify-full&sslrootcert=../global-bundle.pem&schema=public
# For AWS RDS remember to URL-encode special characters (e.g. '#' -> %23)
# and point sslrootcert at your downloaded global-bundle.pem CA bundle.
DATABASE_URL=
JWT_SECRET=